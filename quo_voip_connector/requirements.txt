# Core runtime
requests>=2.31.0
click>=8.1.0
python-dotenv>=1.0.0

# Claude direct API integration (optional but recommended)
anthropic>=0.40.0

# MCP server (required for Claude Code / Claude Desktop integration)
mcp>=1.0.0

# Development / testing
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
responses>=0.25.0
ruff>=0.4.0
