# CLAUDE.md — Zest FieldRoutes MCP

Context file for Claude Code / any agent working in this repo. Read fully before changing anything.

## Goal

A remote MCP server that gives Claude (claude.ai web/mobile, Cowork, Claude Desktop) read **and write** access to Zest Lawn & Pest's FieldRoutes account. Priorities, in order:

1. Answer office questions fast: which property is assigned to which route/tech, what's due for service, what a tech must know before a stop (Red Notes, flags, alerts), who owes money.
2. Take safe actions: notes, tasks, schedule/reschedule/cancel/complete appointments, hold a spot, update Red Notes, change subscription scheduling (frequency, billing frequency, seasonal window, custom date, region, preferred tech/day/time).
3. Reach every FieldRoutes endpoint without bloating context: 31 curated tools by default, all 187 endpoints via generic tools, optional per-endpoint generation.
4. Never surprise the owner: deletes and real card charges are blocked unless explicitly enabled; writes can be switched off per deployment, or restricted to a test customer while validating a new deployment.

## Non-goals

- Not a general FieldRoutes SDK. Tool outputs are trimmed to what an office user reads, not full records (use `get` for full records).
- Not multi-tenant. One FieldRoutes office per deployment; run a second Railway service for another office or a read-only variant.
- Not OAuth. Access control is a secret path segment (claude.ai's connector UI has no header field). Treat the URL as a password.
- No API field controls FieldRoutes' "Autoschedule" (Due Date Method) setting, or lets you define the dates of a Custom Schedule (only pick an existing one by ID). Both stay UI-only; don't invent params for them.

## How FieldRoutes' API actually works (do not "fix" these)

Built from the official Swagger spec (`src/fr_mcp/fieldroutes_spec.json`, extracted by `scripts/extract_spec.py` from `https://api.fieldroutes.com/resources/swagger.js`, which is itself the real spec, not a mock). Every earlier third-party MCP got this wrong:

- `POST https://{subdomain}.fieldroutes.com/api/{entity}/{action}` — `.pestroutes.com` also works (`FR_BASE_URL` override).
- Body is `application/x-www-form-urlencoded`, **not JSON**. `authenticationKey` and `authenticationToken` go in the body. **httpx 0.28 gotcha:** `AsyncClient.post(url, data=list_of_tuples)` silently degrades to a sync-only content stream and crashes an async client (`data=` is only treated as form-urlencoded when it's a `Mapping`). `client.py` pre-encodes with `urlencode()` and sends via `content=` plus an explicit `Content-Type` header instead. Don't switch this back to `data=`.
- Arrays are PHP-style repeated keys: `customerIDs[]=1&customerIDs[]=2`.
- Search filters are JSON strings: `balance={"operator":">","value":"0"}`. Operators: `> < >= <= = != IN BETWEEN LIKE STARTSWITH ENDSWITH CONTAINS`. A plain list in a search filter is sent as an `IN` object (`client.normalize_search_filters`).
- Responses are JSON; HTTP is 200 even on failure (`success: false` + `errorMessage`). **Verified live on `office/search` and `office/get`**, every response also carries an envelope the swagger doesn't document: `params` (an echo of the *entire request, authentication key and token included* — `client.call()` strips it before returning, never reintroduce it into tool output), `tokenUsage` (FieldRoutes' own `requestsReadToday`/`requestsWriteToday` — authoritative, shared across integrations, survives our restarts; `UsageCounter.sync()` adopts it over our estimate), `tokenLimits`, `requestAPIKeyType`, `requestAction`, `endpoint`, `ignoredParams` (a **list**, and it precedes the data key — so "first list in the body" can never be how records are found), `processingTime`, `count`. `search` returns the ID list under `{entity}IDs` (singular entity name; `idName`/`propertyName` both name that key) and, with `includeData=1`, the first 1000 resolved records under the **plural** entity name (`offices`, not `office`) named by `propertyNameData`, plus `{entity}IDsNoDataExported` for the rest, which come via `get` in chunks of 1000. `get` returns records under the plural name too, named by `propertyName`. `client.extract_records()` trusts those name hints first, then plural/singular, then a metadata-aware scan — don't shortcut it.
- **`get`'s bulk-ID param is *not* always `{entity}IDs`.** 24 of 56 entities deviate: `serviceType/get` takes `typeIDs`, `cancellationReason/get` takes `reasonIDs`, `customerFlag/get` takes `customerIDs` (it has no ID of its own — its "get" returns every flag row for those customers), `applicationMethod/get` takes the FieldRoutes-side-misspelled `applciationMethodIDs`, and 20 more. `extract_spec.py` derives the real one per entity (every `get` endpoint has exactly one array-typed param — that one) into `entities.{entity}.getIdParam`; `server._id_param(entity)` reads it, defaulting to `{entity}IDs` when unset. Always fetch through `_get_rows`/`_search_rows`, never hand-roll a `{entity}IDs` key.
- FieldRoutes' own swagger has real defects, not just gaps: `compassCustomer/search` has three params with `name: 0/1/2` (types `"o"`/`"c"`/`"d"`, evidently a string exploded character-by-character by their spec generator). `extract_spec.py`'s `slim_param` drops any param whose name isn't a valid Python identifier rather than crash tool registration. If a future re-extraction adds new junk like this, it'll be silently dropped the same way — check `describe_endpoint` output if an entity seems to be missing an obvious param.
- Rate limits: 60 req/min/office; 3,000 reads + 3,000 writes/day, **shared with every other integration touching the office** (FieldRoutes' own UI, Zapier, website forms). `client.py` has a sliding-window rate limiter at 55/min, and a `UsageCounter` that tracks this server's own reads/writes per local day and refuses new calls of a kind at 95% of `FR_DAILY_READ_LIMIT`/`FR_DAILY_WRITE_LIMIT` (default 3000 each) rather than running the office's shared quota to zero. This is a self-imposed estimate, not authoritative — FieldRoutes' server is.
- Field names that matter: customers use `fname`/`lname`/`phone1`; the customer's **Red Notes = `customer.notes`**, permission `editRedNotes`. **Verified live** (Zest customer 10000, 2026-09-03): despite the published spec declaring `notes` write-only and omitting it from `customer/get`'s response schema, the real API *does* return it on `customer/get` once a value has been set -- round-tripped a test string through `set_red_notes` → `customer_alerts` (which reads `customer.notes` via `_get_rows("customer", ...)`) and got it back verbatim, then cleared it back to empty. `customer_alerts`/`set_red_notes`'s existing blind-overwrite implementation is correct as shipped; no read-before-write revisit needed. Don't confuse this with an office's own custom Note Type that happens to be *named* "Red Note" (e.g. Zest's is typeID 8, Admin → Preferences → Note Types -- per-office config, not a fixed ID) -- that's a normal timestamped `note` entity row, a completely separate mechanism from the `customer.notes` banner field. Confirmed unrelated: writing `customer.notes` via `set_red_notes` created zero new rows in `list_notes`.
- **Verified live on `customer/get`**: every field value comes back as a JSON *string*, even numeric/ID ones (`"customerID": "10000"`, `"balance": "0.00"`) — `_int()`/`float()` handle this fine, no fix needed. More importantly, `customer.subscriptionIDs` and `customer.appointmentIDs` are **comma-separated strings** (`"appointmentIDs": "10002,10006,16404"`), not JSON arrays, despite the swagger's declared `type: array` — same pattern as `additionalTechs`. Passing that string straight into `_get_rows()` used to iterate it character-by-character (a real bug this caught: `"12,34"` silently became IDs 1, 2, 3, 4 instead of 12 and 34, which for a customer with a multi-digit or second subscription ID would fetch the wrong subscription). Always route a customer's own ID-list fields through `_id_list()`, never index them directly. Also: the live record carries several fields the swagger doesn't declare at all (`customerNumber`, `lotSize`, `subPropertyType`, `customerSubSourceID`/`customerSubSource`, `districtCode1`-`5`) — the spec's field list is a floor, not a ceiling; don't assume it's exhaustive when reading a live record.
- Scheduling notes = `specialScheduling` (on `customer`, read-only from the tools' perspective — no curated write for it yet). Alerts = `task` with `type=1` (`type=0` is a plain task); task `category`: Billing 1, Customer Care 10, Appt Status 15. Notes need `contactType` (Note Type ID from Admin → Preferences → Note Types; no API endpoint lists them) — note's own row ID in write params is **`contactID`, not `noteID`** (`note/update`, `note/delete`). Cancel uses `cancelReason` (string) + `cancelledBy` (employeeID). Appointment `status`: 0 pending / 1 completed / 2 no-show. Subscription `active`: 1 active / 0 frozen / -3 lead. Routes/spots expose `apiCanSchedule`. `appointment.additionalTechs`/`route.additionalTechs` are **comma-separated strings** of employeeIDs, not arrays (confirmed against the spec's own type declaration) — `_emp_list()` splits on `,`. `customer/get` takes `includeCustomerFlag=1` to inline the customer's flags (array under `customerFlag`); flags are also independently reachable via the `customerFlag` entity (`customerIDs`/`customerFlags` filters).

## Repo layout

```
src/fr_mcp/
  client.py             HTTP client: form encoding, auth, retries, rate limit, daily usage quota, search+get pagination
  server.py             MCPServer, 31 curated tools, generic tools, write guards + allowlist, HTTP app + entrypoint
  generated.py           Optional: one typed tool per endpoint, built from the spec at startup
  fieldroutes_spec.json Slimmed Swagger: 187 endpoints (params, required, descriptions) + entity field lists + getIdParam overrides
scripts/extract_spec.py  Regenerates fieldroutes_spec.json from FieldRoutes' public swagger.js
tests/test_wire_format.py  Fake FieldRoutes (httpx.MockTransport) verifying the wire format, pagination, quota, and allowlist
Dockerfile, railway.json   Railway deploy (healthcheck /healthz, installs from requirements.lock)
requirements.lock          Pinned runtime deps; regenerate deliberately, see README
.github/workflows/ci.yml   pytest + a real `docker build`/`docker run`/`/healthz` smoke test on every push and PR
.env.example               Every env var, commented
README.md                  Deploy steps + first-run checklist (user-facing)
docs/lead-scraper-plan.md  Design doc for the lead scraper below: data sources, scoring, sheet schema, FieldRoutes handoff, phases, validation checklist
src/fr_mcp/leads/          Lead scraper (separate `fr-leads` CLI, not an MCP tool): arcgis.py (Sacramento ArcGIS feed + ICP mapping),
                           myhd.py (Placer/Yolo inspection-portal JSON adapter -- the only bulk source for those two counties),
                           classify.py (pest classification, county-agnostic), score.py (pure scoring, county-agnostic),
                           regions.py (zip->region, distance, zip centroids for Placer/Yolo), reports.py (PDF fetch/cache/parse,
                           Sacramento + Yolo header layouts), pipeline.py (Sacramento pull+filter+score orchestration),
                           sheet.py (Google Sheet destination writer -- the system of record for prospects, dependency-injected
                           SheetBackend so it's unit-tested without live Google credentials), fr_push.py (FieldRoutes dedupe +
                           writes, reuses server.py's guards, used only at the "inspection booked" handoff), cli.py (`fr-leads`
                           entrypoint: `run --destination sheet|fieldroutes`, `preview --counties ...`, `push`, `food`)
src/fr_mcp/leads/          Food Facilities tab (`fr-leads food`, a second call list on the same sheet): cdfa.py (CDFA Market
                           Enforcement licence registry -- an ASP.NET WebForms POST that answers with a CSV), calepa.py
                           (CalEPA site portal's undocumented JSON/CSV API, NAICS-filtered), fsis.py (USDA meat/poultry
                           directory, with an Internet Archive fallback for the blocked host), food.py (FoodFacility model,
                           name-before-NAICS classification, retail exclusion, address-based merge)
tests/test_leads_*.py, tests/leads_*.py  Lead scraper tests + fixtures (real inspection-report text incl. a real cached Yolo
                           report, a FakeFR subclass that persists writes -- see its docstring for why the shared `fake`
                           fixture isn't enough -- and FakeSheetBackend for sheet.py, mirroring the same test-double pattern)
```

## Tool inventory (server.py)

Generic: `list_endpoints`, `describe_endpoint`, `search`, `get`, `call`, `health_check`.
Reads: `find_customer`, `customer_360`, `customer_alerts`, `day_schedule`, `property_assignments`, `route_stops`, `crew_schedule`, `due_for_service`, `open_slots`, `ar_aging`, `lookups`, `subscription_details`, `list_notes`.
Writes: `add_note`, `update_note`, `set_red_notes`, `create_task`, `schedule_appointment`, `reschedule_appointment`, `update_appointment_notes`, `cancel_appointment`, `complete_appointment`, `reserve_slot`, `update_subscription`.
Reads + writes combined: `service_schedule` — appointment history/upcoming grouped by subscription and zone (region), with an optional `move` list to reschedule one or more of those appointments (delegates to `reschedule_appointment` per entry, so it shares that tool's guards and its `spotID`-not-a-bare-date semantics) in the same call.

That's 31. Keep this count and the list in sync with `server.py` and README's tool table — the HTTP app test (`test_http_app_secret_path_healthz_and_bearer`) asserts the exact count and will fail the moment they drift.

Naming: curated tools are verbs/nouns an office person would say. Generated tools are `fr_{entity}_{action}`.

## Conventions

- Every tool returns a JSON string via `_j()`. Trim records with `_pick(row, KEYS)`; the `*_KEYS` lists at the top of `server.py` define what an office user sees. Add a key there rather than dumping whole records.
- Curated reads should resolve IDs to names (`_emp_names`, customer lookups) and put the **property address** on any row that has a customer. When you already have the customer record in hand (e.g. `customer_360` fetching its own appointments/subscriptions), pass it into `_shape_appointments`/`_shape_subscriptions`' `customers` map instead of letting them re-fetch — a redundant `customer/get` costs a real call against the shared daily quota.
- Dates: `YYYY-MM-DD`; datetimes `YYYY-MM-DD HH:MM:SS`, office local time (`TZ` env var — Railway defaults to UTC). Default windows are today + 6 days.
- `FR_OFFICE_ID` is auto-applied to searches when set and no office filter is given.
- Writes go through `_writes_enabled()`, then `_require_customer_allowed()` (the `FR_WRITE_CUSTOMER_IDS` allowlist — see below). Anything that could cancel, charge, freeze, or overwrite Red Notes must confirm with the user (say so in the docstring — Claude reads it).
- **Write allowlist** (`FR_WRITE_CUSTOMER_IDS`, comma-separated customer IDs): when set, a write is refused unless it resolves to one of those customers. Direct-customer-ID tools (`add_note`, `set_red_notes`, `create_task`, `schedule_appointment`) check immediately; record-based tools (`update_note`, `reschedule_appointment`, `update_appointment_notes`, `cancel_appointment`, `complete_appointment`, `update_subscription`) fetch the existing record first via `_write_target_customer(entity, id)` — which is a no-op returning `None` instantly when the allowlist isn't set, so this costs nothing once the deployment goes live and the variable is cleared. `reserve_slot` is the one deliberate exemption: a spot hold names no customer and isn't customer data. The generic `call` tool and every generated tool go through the same `_resolve_write_customer()`/`_require_customer_allowed()` pair — fail closed: an entity with no customer relationship at all (route, spot writes other than reserve, employee, service type, ...) resolves to `None` and is refused when the allowlist is active, not silently allowed through.
- Generic `call` and generated tools share guards: `*/delete` needs `FR_ALLOW_DELETE=1`; `payment/create` with `doCharge=1` needs `FR_ALLOW_CHARGES=1`.
- Errors: raise `FieldRoutesError`; the `_tool` wrapper (curated tools) / inline `try/except` (generic `call`, generated tools) converts it to `ToolError` so the message reaches Claude.
- Tool docstrings are the schema descriptions. One or two sentences; say when to use it and what it returns. Context cost matters: 31 curated tools measure ~24.7k chars of schema JSON (~6.2k tokens at a rough 4-chars/token estimate; `test_context_budget.py` pins this with headroom) -- higher than a naive per-tool estimate because pydantic wraps every optional `X | None = None` param in an `anyOf`/`title` structure. `FR_EXPOSE_ALL` (187 endpoints) is roughly 10x that.
- mcp SDK is **2.x** (`mcp.server.mcpserver.MCPServer`, not `FastMCP`; `ToolError` is `mcp.server.mcpserver.exceptions.ToolError`). Transport is streamable-HTTP, stateless, JSON responses, DNS-rebinding protection off (Railway hostnames). `mcp.call_tool()` returns a `CallToolResult` with `.content[0].text`, not a subscriptable list — if you're writing a test against it directly, don't index it.
- Generated tools are built with a dynamically-constructed `inspect.Signature` (real `inspect.Parameter` objects with proper annotations) attached to a `**kwargs`-accepting async function, not a hand-written one per endpoint. This is required for the MCP SDK's Pydantic-based schema derivation (`Tool.from_function`) to produce an accurate per-tool JSON schema, `required` fields included, from spec data at startup.
- Never log the HTTP request path in the `http` transport — it contains `MCP_PATH_SECRET`. `main()` runs uvicorn with `access_log=False` and never prints the resolved secret path (`/<secret>/mcp` in the startup log, not the real value). Don't reintroduce either.

## Environment variables

| Var | Purpose |
| --- | --- |
| `FR_AUTH_KEY`, `FR_AUTH_TOKEN` | FieldRoutes Admin → API → Manage Keys |
| `FR_SUBDOMAIN` / `FR_BASE_URL` | Tenant host |
| `FR_OFFICE_ID` | Default office filter |
| `FR_DEFAULT_EMPLOYEE_ID` | Employee the API acts as (notes, tasks, appointments) |
| `FR_DEFAULT_NOTE_TYPE_ID` | Note Type for `add_note` |
| `FR_DEFAULT_TASK_CATEGORY_ID` | Task Category for `create_task` (required by FieldRoutes, per-office; `lookups(kind="task_categories")`) |
| `FR_WRITES` | `on` (default) / `off` |
| `FR_ALLOW_DELETE`, `FR_ALLOW_CHARGES` | Unlock destructive actions |
| `FR_WRITE_CUSTOMER_IDS` | Comma-separated customer IDs writes are restricted to; unset = unrestricted. Use during first-run validation. |
| `FR_DAILY_READ_LIMIT`, `FR_DAILY_WRITE_LIMIT` | Self-imposed daily call budget (default 3000 each); refused at 95%. |
| `FR_EXPOSE_ENTITIES`, `FR_EXPOSE_ALL`, `FR_EXPOSE_VERBOSE` | Generated per-endpoint tools |
| `MCP_TRANSPORT` | `http` (Railway) / unset (stdio, Claude Desktop) |
| `PORT`, `MCP_PATH_SECRET`, `MCP_BEARER_TOKEN` | HTTP hosting + access control |
| `TZ` | Office local time zone |

## Dev loop

```
python -m venv .venv && . .venv/bin/activate
pip install -e ".[dev]"
pytest                                  # fake-API wire-format tests, must stay green (58 tests)
MCP_TRANSPORT=http FR_AUTH_KEY=x FR_AUTH_TOKEN=y FR_SUBDOMAIN=z MCP_PATH_SECRET=s fr-mcp
curl localhost:8080/healthz             # -> ok
```

To smoke-test tools without credentials, inject `FieldRoutesClient(transport=httpx.MockTransport(FakeFR().handler))` into `server._client` (see tests). `tests/test_wire_format.py`'s `FakeFR` seed data models the real deviations above (a `customerFlag` with no ID field, `additionalTechs` as a comma string, entities whose `get` id param isn't `{entity}IDs`) — extend the seed rather than special-casing around it if you add a test that needs new fixture data.

## Adding a tool

1. Check `describe_endpoint`/the spec for exact param names. Do not invent names — and don't assume `{entity}IDs` for a bulk-get id param; check `entities.{entity}.getIdParam` or just call `describe_endpoint`.
2. Write it next to similar tools in `server.py`, decorated `@_tool` (not `@mcp.tool()` directly — `_tool` wraps `FieldRoutesError` into `ToolError`), returning `_j(...)`.
3. Resolve IDs to names, include the property address, trim with `_pick`.
4. If it writes: call `_require_writes(tool)`, then `_require_customer_allowed(tool, customer_id)` — either directly (if the tool already takes `customer_id`) or via `await _write_target_customer(entity, record_id)` if it only takes an existing record's ID. If it can cancel/charge/delete/freeze/overwrite Red Notes, say "confirm with the user" in the docstring.
5. Add the tool to the README table, this file's inventory (and count), and `test_http_app_secret_path_healthz_and_bearer`'s expected tool count. Run `pytest`.

## Lead scraper (fr-leads)

A separate console script (`src/fr_mcp/leads/`, entry point `fr-leads`), not an MCP tool -- it never imports `server.py`'s `MCPServer`/tool registrations, so a run stays cheap and the MCP server stays unaware it exists. Its FieldRoutes path (`fr_push.py`, used only at the "inspection booked" handoff, or via `--destination fieldroutes`/`push` for validation) reuses `server.py`'s write guards and client (`server.client()`, `_search_rows`, `_get_rows`, `_require_writes`, `_require_customer_allowed`, `_default_employee`/`_default_note_type`/`_default_task_category`) directly rather than a second copy of them, so `FR_WRITES`, `FR_WRITE_CUSTOMER_IDS`, and the daily quota apply to it exactly as they do to every curated tool.

Full design lives in `docs/lead-scraper-plan.md` (Revision 2) -- read it before touching this code, especially sections 2.6 (Placer/Yolo), 3 (scoring), 5 (the sheet schema and the FieldRoutes handoff) and 5.2/Appendix A.2 (dedupe). Shipped: all three counties, the sheet destination (`sheet.py`, the default), the FieldRoutes destination (`fr_push.py`, the phase-1 behaviour, now handoff-only), and Google Places phone enrichment. Not yet built: website/ZoomInfo enrichment, `lead_preview`/`lead_import` MCP tools.

The **Food Facilities** tab (`docs/food-facilities-plan.md`, section 13b for what live recon changed) is a second call list on the same spreadsheet -- processors, bakeries, breweries, wineries, cold storage, food distribution -- built from licence registries rather than inspection feeds. Live since 2026-09-09: 281 facilities, 264 with a phone.

A few things worth knowing before changing this code:

- **The sheet, not FieldRoutes, is the system of record for prospects** (plan Revision 2's central decision). `cli.py`'s `run` defaults to `--destination sheet`; `--destination fieldroutes` is the original phase-1 path, kept for the handoff and for validation (`push`). Don't reflexively route new lead-creation logic through `fr_push.py` -- check which destination the change actually belongs to.
- **`customerLink`/the sheet `key` column's prefix names the source**: `SACEMD:<Facility_ID>` (Sacramento, from the ArcGIS feed), `PCHD:<PR permit number>` (Placer, parsed from the portal row's `permitName` -- its report PDF has no owner/phone/email at all, so `myhd.py` never fetches it), `YOLO:<FA id>` (Yolo, from its report PDF header -- fetched for every surviving candidate anyway because that PDF is also Yolo's email source). FieldRoutes dedupe on this key is checked by exact match first, then an address+zip `CONTAINS` fallback, then an exact 10-digit phone match (`fr_push.find_existing_customer`); a hit found via the fallbacks gets `customerLink` backfilled via `customer/update`, but only when the existing value is empty (`fr_push._adopt_customer_link`) -- never overwrite one a human or an earlier run set. The sheet's own dedupe (`sheet.sync_leads`) is simpler: the key already existing as a Leads-tab row is enough.
- **A lead is idempotent by inspection GUID, not by date, in both destinations.** FieldRoutes: `fr_push.already_noted` reads every note on the matched customer and looks for the signal's `pkey` inside the note text. The sheet: `sheet.sync_leads` reads the Signals tab for `(key, guid)` pairs already recorded. Either way, that's what makes re-running the same day (or a whole run re-running after a crash) safe with no other local state. Territory-lane leads (no signal, no GUID) skip this entirely -- once created, a second run just sees the key already present and does nothing (FieldRoutes: `push_candidate`'s `LANE_TERRITORY` branch; sheet: `sync_leads`'s `skipped_no_change`).
- **Placer and Yolo's permit-type vocabulary is classified by regex, not an exact-string table** (`myhd._placer_icp_base`/`_yolo_icp_base`), because the real live phrasing has more size/seat-range variance than `docs/lead-scraper-plan.md` section 2.6's own summary table shows (e.g. a real Placer row reads "Market - With Food Prep < 5000 Sq Ft", not the "Equal To Or > 5000"/"< 500" phrasing the table documents). If a live pull surfaces a permit type these regexes don't recognize, they exclude it rather than guess -- check `describe`/log output if a facility you expected to see is missing.
- **FieldRoutes returns every field as a string, even on create/write params you sent as ints** -- confirmed by `tests/leads_persistent_fake.py`'s `PersistentFakeFR`, which stores raw form values (all strings) the same way the real API's wire format does. Comparing a stored field against a bare Python `int`/`bool` in a test is the same bug CLAUDE.md warns about elsewhere in this file (`_int()`/never truth-test a raw field) -- it bit this code three times while it was being built (see the git history on `fr_push.py`/`classify.py`/`arcgis.py` if you want the specifics): a permit-type lookup key with a trailing space vs. a stripped one, a phone regex that matched the *inspector's* phone instead of the facility's because it searched the whole PDF instead of just the header block, and `has_open_task` filtering on `status == 0` (pending) alone, which missed Hot leads' tasks (created with `status 3`, urgent) and would have stacked a duplicate task on every retouch.
- **Which key a `create` response uses for the new row's ID is unverified live**, especially for `task` (this file already notes elsewhere that a note's own ID field is `noteID` on reads but `contactID` on writes -- the same kind of inconsistency is plausible for a freshly created task, whose *read* field is `taskIDs`, not `taskID`). `fr_push._extract_id` tries several plausible key spellings rather than assuming one; this is logged/state-only, never load-bearing for correctness (the write itself already succeeded by the time it's called).
- **The territory lane's "park" score label doesn't mean "don't push."** `pipeline.LeadCandidate.pushable` treats the event lane and the territory lane differently on purpose: an event-lane candidate with no real pest signal legitimately shouldn't be pushed (tier "park"), but a territory-lane candidate has no pest signal *by definition* (it's an audit-offer prospect, not a complaint) and can still score under 30 purely from geography -- it stays pushable once it clears the `icp_fit >= 24` gate at build time. Getting this backwards silently drops most of the territory lane; see the docstring on `pushable` before changing the tier logic. `myhd.build_candidates` builds the same `LeadCandidate` dataclass directly (rather than forcing Placer/Yolo data through `arcgis.Facility`), so this property applies identically there.
- **The Leads tab's tool-owned and rep-owned columns are disjoint by name, enforced in `sheet.py`, not by position.** `sync_leads` writes `TOOL_COLUMNS` only, ever -- the one exception is `status`, set to `"new"` at first append and never touched again. Don't add a new tool-written field without adding its column name to `TOOL_COLUMNS`; a column that's in neither list will read back blank forever.
- **Every pushable Sacramento candidate fetches one report PDF; the PDF header is the only free phone source.** The feed carries no owner or phone. `pipeline.build_candidates` decides the lane first, then fetches the signal's report for the event lane and the facility's latest inspection report (layer 0's own `Inspection_Report`, kept on `Facility.latest_report_url`/`latest_pkey`) for the territory lane; parked chains fetch nothing. The first live run only fetched for vermin-category signals and put 40 rows in the sheet with one phone between them -- don't reintroduce that gate. Measured live 2026-09-08 across 243 fetched reports and 160 sheet rows: **143 of 158 Sacramento rows (90%) carry a phone** and 150 an owner; the 15 without are hotels, country clubs and chains whose header phone is genuinely blank (SEAPOT's was confirmed blank against the live report back in phase 1). Placer is 0% by design -- its report carries no phone at all, so those rows wait on Google Places (phase C). `sheet.sync_leads` backfills contact columns still blank on a known row (`enriched` in the run summary) and never overwrites a value already there. On Railway the cache lives on a volume (`LEADS_PDF_CACHE_DIR=/data/pdf`, volume mounted at `/data`) so the ~300 first-run fetches happen once, not every morning.
- **The county's phone is not reliably the business's line, and Places is the second opinion.** Audited live 2026-09-08, 30 Sacramento `report_pdf` rows sampled against Places: 21 matched, 9 differed, 0 failed to match. Every disagreement had a Places hit at the *identical* street address with a close name, so the county field was the wrong one rather than Places finding a neighbour -- extrapolating, ~26% (about 37 of 143) carry a number that is not the business line. Failures cluster on small independents (large/chain/institutional was 5/5 correct) and, most usefully, **every out-of-area number sampled (3/3) was unusable**: a Long Island number on a Folsom restaurant, a Seattle number shaped like a placeholder (800-9999), and a local number with its area code transposed (210 for 510). So: the county number stays primary (for a cold call the owner's direct line beats a switchboard), `business phone` carries the Places number beside it, and `regions.is_local_number` flags anything outside 916/279/530 in `phone flag` -- the cheap half of the problem, caught with no API call. Two cautions from the same audit: some numbers repeat across two facilities (reps double-call), and Places' `matched_name` is user-editable vandalism bait (one real listing came back "Price Gouging Market"), so it is never written to the sheet.
- **Places is the only billed step in this pipeline.** `_worth_enriching` therefore looks up exactly the rows a lookup pays off on -- those already in the sheet plus the new ones the row cap will admit -- because a candidate past the cap is discarded before it is written and re-built next run. `keys_with_business_phone` stops a row being re-priced every morning, and `PlacesClient` carries a hard per-run ceiling plus a circuit breaker on 401/403/429 (those mean every later call fails identically). Auth is the *service account*, not an API key: Places API (New) accepts an OAuth `cloud-platform` token, so there is no second credential to store or restrict, which matters because Railway hands out no static egress IP to allowlist below the Pro plan.
- **A returned place is only trusted when its address agrees with the county's** (`places.is_same_place`: zip match, or a whole-word house-number match). Text search returns *a* plausible business for a bad query, and a wrong phone is worse than a blank one -- the rep burns the lead calling a stranger.
- **The Leads tab's column order is a separate concern from ownership.** `TOOL_COLUMNS`/`REP_COLUMNS` decide what may be written; `LEADS_COLUMNS` decides the order, and it opens with `facility, phone, notes, followup` because a rep on the phone reads left to right and must not scroll to dial. `reorder_leads_tab` applies that to an existing sheet (and collapses duplicated columns, since only the leftmost copy is ever kept current); it rewrites the tab wholesale, so keep it out of the run loop, where a crash midway would truncate rather than leave the sheet untouched. `ensure_columns` is the in-run counterpart: it appends a newly shipped tool column at the far right, without which every write to it is silently dropped.
- **Every write to the sheet goes out batched; Sheets allows 60 write requests per minute per user.** `GspreadBackend.update_rows` puts every cell of every row into one `batch_update`. The original cell-at-a-time `update_row` was ~280 requests to backfill 40 rows and took an HTTP 429 partway through, half-writing the sheet. `read_rows` also builds its dicts from raw values rather than `get_all_records()`, which raises outright on a duplicated header -- a rep copying a column killed a whole run that way; a duplicated *tool-owned* column is now reported in the run summary and the Runs tab instead, since an append fills every copy but an update only touches the leftmost.
- **`PoliteFetcher` sniffs the response body for `%PDF`, not the Content-Type header.** Verified live 2026-09-08: the portal serves some reports as a valid PDF with no Content-Type header at all. The original header-only check read the first such report as a captcha page, tripped the circuit breaker, and silently returned None for every report after it -- a whole run fetched exactly one PDF and looked "fine" (exit 0, no errors). A 200 with an HTML body still counts as a block; that's the real captcha case. If a run's report count looks suspiciously low, check `fetcher.blocked` before anything else.
- **`fr-leads preview --counties placer,yolo` verified live 2026-09-08** against the real portal (a 3-day window, `--no-fetch`): both counties returned clean 200/JSON, no captcha, and a real facility (ZEBRA TOWN DELI & MARKET, Lincoln) came through with the correct lane, ICP tier, region, and distance.
- **The sheet destination is live, 2026-09-08.** A Railway cron service ("Lead Scraper" in the `serene-essence` project, weekdays 7am Pacific, `fr-leads run`) is deployed from this branch with `LEADS_SHEET_ID` and `GOOGLE_SERVICE_ACCOUNT_JSON` set -- it needs no `FR_*` variables at all, since a sheet-destination run never constructs a `FieldRoutesClient` (confirmed by reading `client.py`: those env vars are only read inside `FieldRoutesClient.__init__`, never at import time). Railway's cron model has no on-demand "run now": a `redeploy` only re-validates the build and produces zero deploy-log output, it does not invoke the start command -- confirmed by checking `get-logs` after both the initial deploy and a `redeploy`. The first real run was instead triggered by running `fr-leads run` directly against the live sheet with the same credential: **40 rows added, 0 errors**, across Sacramento (event lane Hot down to Cool, one territory-lane row) and Placer (one event-lane row, `PCHD:PR0007074`), 274 more pushable candidates correctly held back by the new-row cap, every tool-owned column filled and every rep-owned column blank. See `docs/lead-scraper-plan.md` section 11 item 1 for the full result; items 3-7 are still open.

- **The first unattended Railway run (2026-09-09, cron `0 14 * * 1-5`) worked, and taught two things.** It added 40 rows with `errors: []`, columns A-D intact, no duplicate headers -- but two problems only a scheduled run could show:
  1. **Sacramento's PDF pass starved Placer and Yolo, silently.** Both counties and Sacramento's report PDFs come off the same myhealthdepartment.com host, which rate-limits by IP. A cold cache meant ~150 report fetches at one every two seconds; five minutes in, the portal started answering 403, and Placer's very first search inherited it and tripped the circuit. Both counties contributed **zero rows while the summary still said `errors: []`** -- every layer soft-fails by design, which is right for the write path and wrong for the summary. `_build_all` now pulls Placer and Yolo **before** Sacramento (their searches are a handful of requests, so the order costs nothing), and `_pull_warnings` turns a tripped circuit, a blocked report endpoint or an empty county into a reported error. The `pull` block in the run summary carries per-county counts plus `reportsFetched`/`reportCacheHits`, so a cache volume that stops persisting is visible rather than inferred.
  2. **One stale Yolo row was repaired by hand.** `YOLO:D0F86945-...` (LA ESTRELLITA MEAT MARKET, first seen 2026-09-08) predated the `_address_key` fix and carried a permit GUID as its key -- so its next inspection would have arrived as a brand-new lead. Today's code keys it `YOLO:FA0014697` (confirmed by re-running the Yolo pull for it), and the sheet cell was updated to match; no rep column was touched. Today's run added no Yolo rows at all, so nothing new regressed.
  3. **Places was re-buying the same unresolvable rows every morning.** `keys_with_business_phone` skipped rows that *had* a business phone, so the 45 rows Places could not resolve -- bad address, no listing, name too generic -- were looked up again on every run, and spent the whole 50-call budget doing it. "Asked and got nothing" is now recorded too, in a `places checked` column on both tabs; the skip set is "has a number **or** was already asked". Clear the cell by hand if a row ever deserves a second look.

## Apartments and Property Managers (`fr-leads apartments`)

Two tabs from one Places sweep, live 2026-09-09: **803 apartment properties** (757 with a phone, **569 with a posted leasing office**) and **39 management companies holding 198 of them**, top portfolio 30 properties.

- **The assessor route is dead, and that is the finding that shaped everything else.** The obvious source is the parcel roll -- owner, mailing address, unit count -- with LLCs clustered by shared mailing address. Checked live: Sacramento County's public parcel layer publishes APN, address, land use and lot size and **no owner, no mailing address, no unit count**. That kills ownership clustering *and* the "16+ units means a resident manager under Title 25" filter, since nothing public carries units. (The layer is still useful for scale: 3,516 "Low Rise Apartment" and 91 "High Rise" parcels in the county.)
- **Posted leasing-office hours replace the unit-count rule, and are strictly better.** "16+ units" was only ever a proxy for "somebody is there in the daytime"; `regularOpeningHours` observes it directly and a rep can read the actual hours before driving over. 569 of 803 have them.
- **Review count is the size proxy and is labelled as one.** No public source carries unit counts. The `size` column buckets review volume; the sheet says `reviews`, never `units`.
- **Manager portfolios come from a shared website domain, or failing that a shared local phone.** The domains *are* the companies -- `usamfm.com` is USA Multifamily (30 properties), `tandemproperties.com` (14), `kjaxproperty.com` (13), `buckinghampm.com`, `lewisapartments.com`. So a domain cluster is named for its domain: naming it after its biggest property would call 30 properties "Terracina at Park Meadows".
- **Three things deliberately do not cluster.** A per-property branded domain (`elevatetolarkspurwoods.com`) -- correct, it is evidence of nothing. A listing portal (`apartments.com`, Zillow, a Wix subdomain) -- twenty unrelated properties link there. A shared **toll-free** number -- 800/833/844 is a call centre, and two properties on one may be one manager or the same lead-capture vendor sold to both. A shared *local* number does cluster.
- `grouped by` on each manager row says which inference formed the group, so a rep can weigh it. National operators (Greystar, FPI, Essex...) are kept and flagged, the same call the Leads tab makes about grocery chains.
- The properties tab sorts staffed-office-first, then biggest, because the ask was for the large ones a rep can walk into.

## Marking institutional rows (`fr-leads mark-institutions`)

Shades nursing/care, hospital/medical and apartment rows red on both tabs and records what each one is in a `facility class` column. Colour is what a rep reads without reading, but it survives no round trip -- nothing can later ask the sheet "which rows are care homes", and a rep who copies a row loses it -- so the colour and the column are always written together.

**The important finding, and it is about the data rather than the code: the inspection feeds contain almost none of these.** A full 365-day pull of all three counties yields **4 rows** -- two Eskaton senior-living communities, Sacramento Post-Acute, and Mercy San Juan's hospital kitchen. That is not a lookback artefact:

- These are **food-facility** feeds. A care home appears only if it runs a licensed kitchen; most contract catering out. `LICENSED HEALTH CARE FACILITY` is 3 rows in a whole year across the county.
- **An apartment complex is not a food facility**, so no health-inspection feed will ever list one. Reaching those needs a rental registry or the assessor, not a longer pull.
- A hospital appears under its cafeteria's permit and its cafeteria's name -- Mercy San Juan reads as "SATELLITE FOOD DISTRIBUTION FACILITY".

So this marks what is genuinely there; it is **not** a way to build a care-facility target list. For that, CDPH (hospitals) and CDSS Community Care Licensing (nursing, memory care, assisted living) publish the licensed-facility registries, which would give hundreds rather than four.

False positives were most of the work, and every one below was a live row:

- `ELDER CREEK MARKET` is a market on Elder Creek Road; `Lira Clinical Store` is a skincare brand ("clinical" is not "clinic"). Hence word-bounded, paired patterns -- a bare "care" or "living" matches nothing, or "Loving Care Catering" comes along.
- **Operator brands are needed as well as words**: `ESKATON MONROE LODGE` is a real senior-living community filed under a plain `RESTAURANT` permit, and nothing in the name says care. Brands are either unambiguous (Eskaton, Brookdale) or paired -- "Sutter" alone is a street, a county and a fort here, and "Oakmont" is a golf course as often as a care home.
- **Schools are deliberately not marked.** They were not asked for, and the first attempt produced two false positives out of six matches: `UNIVERSITY OF BEER` is a bar and `DESTINY CHURCH (SACRAMENTO CAMPUS)` is a church.

Where a county filed a permit, the permit beats the name. Re-running is idempotent, and a row that stops matching has its stripe and class cleared rather than left stale.

## Food Facilities tab (`fr-leads food`)

A second call list on the same Google Sheet, live 2026-09-09: **567 facilities, 528 with a phone (93%)**, from three licence registries plus Google's keyword sweep, rather than the county inspection feeds the Leads tab uses. Things a live pull taught that the plan could not:

- **Each source's own category is a hint, not a fact.** CalEPA files `J W AUTO WRECKERS` under NAICS 311 (food manufacturing); CDFA licenses individual produce brokers with no plant at all. `food.category_for` runs the *name* first and only falls back to the registry code, flagging the row when it had to. Dropping on the name alone loses real plants with unhelpful names ("MO West Sac"); trusting the code alone puts a wrecking yard in front of a rep.
- **CalEPA's phone column contains the regulator's number as often as the business's.** Its Affiliations export mixes roles: `CUPA District` is the county environmental health department, and every Sacramento site carries the same (916) 875-8550. Counting those reads as ~100% phone coverage; the real business-line figure is 46%. `calepa.BUSINESS_AFFIL_TYPES` is the allow-list, ordered operator-first -- don't widen it without checking who the affiliation is.
- **CalEPA has no NAICS filter in `/filter/sitefilters`** -- it is an advanced-search key folded into the free-text `term` as `naics_code:"311"`, and it prefix-matches. Anyone reading the filter list concludes the source is unfilterable. Its bad-bbox failure is silent: a wrong projection answers HTTP 200 with zero rows, so `fetch_sites` treats "nothing anywhere" as an error.
- **NAICS prefixes are measured, not assumed.** `4931` (warehousing) returns 56 mostly-Amazon rows; `49312` (refrigerated) returns 7, all cold chain. `4451`/`722` are grocery and restaurants -- the Leads tab's territory.
- **CDFA is one WebForms POST that answers with a CSV**, not an HTML grid. The `__VIEWSTATE`/`__EVENTVALIDATION` pair must come from the GET immediately before it, and `ddlCommodityType` must be the rendered `0`, not blank -- event validation rejects both a stale token and an unrendered value with the same HTTP 500. 5,827 rows statewide, 184 in range, 183 with a phone.
- **`www.fsis.usda.gov` 403s this sandbox** (Akamai, against the egress IP -- not a 404, not fixable with headers). `fsis.py` falls back to the Internet Archive's copy and reports which source answered; "archive" means the data may be weeks old. Whether Railway is blocked too is still unverified.
- **Identity is house number + zip, never the name.** No two sources share an ID, one plant arrives as two CalEPA SiteIDs (Blue Diamond Growers, one row carrying the phone), and names differ across sources ("TONY'S FINE FOODS" vs "TONY'S FINE FOODS/UNFI").
- **Person-held CDFA licences rank below the plants.** Nearly half the in-range rows are individuals; a plain distance sort put four of them above Blue Diamond. `FoodFacility.call_rank` sinks them and `needs_review` says why -- they are never dropped, and a second registry that knows the address as a site promotes the row back.
- **`regions.locate` prefers the zip centroid and falls back to the city's.** A PO-Box-only zip (95851) has no Census ZCTA at all, so a zip-only lookup silently drops most of CDFA's 1,409 PO Box rows. `distance basis` in the sheet says which answered, because a city-centroid distance means "which town", not "which end of it".
- Places is used exactly as the Leads tab uses it for *enrichment*: only rows no registry gave a phone, one lookup per row ever, and the same address-agreement check. The first live pass looked up 39 and matched 27, rejecting 9 whose address disagreed.
- **The keyword sweep (`--sweep`) is a different thing from that enrichment, and spends money differently** -- ~400 requests that *discover* rows, against one per known row. It is opt-in for that reason, with its own ceiling (`LEADS_PLACES_SWEEP_CALLS`). It exists because Google has no place type for food processing, factory, warehouse, distribution centre or cold storage, so a type-driven Nearby Search cannot reach these businesses at all. Measured live: **20 results per page, 60 per query over three pages**, then no `nextPageToken` whether or not the area is exhausted -- so 60 means saturated, and a saturated query is re-run over the rectangle's quadrants (depth-bounded, since each level costs 4x). Recon's "hard 50 cap" was wrong; its "the tail is noisy" was right.
- **`primaryType` is what makes the sweep usable, and its lists are measured, not documented.** A full sweep finds ~2,600 businesses and classifies ~1,300 -- but ~890 of those are unconfirmed guesses. Shipping them would bury the registry rows under a majority of maybes, so only what Google's own type confirms is kept (`--sweep-include-uncertain` adds the rest). Three filters the first sweep's output demanded: **`bakery` is not a confirming type** (Google types a wholesale plant and a cupcake counter identically -- 232 of 588 "confident" rows were bakeries led by Crumbl Cookies and Paris Baguette, so a bakery is confirmed only when its *name* claims production); **a bakery inside a supermarket is not an account** (Safeway Bakery, Costco Bakery); and **name filters fold accents** ("Carnicería Atoyac Meat Market & Taquería" reached the confident list because `\bTAQUERIA\b` does not match "Taquería"). `farm` is dropped too -- the plan's scope is processing, manufacturing and storage, and a grower that packs is typed `manufacturer` anyway.
- **Contact columns are backfill-only; provenance is not.** `sources`, `found via`, `type of business` and the review flag are derived wholly from the run and no rep edits them, so they are refreshed on every pass -- otherwise a row CalEPA found first and the sweep later confirmed still reads "CalEPA" alone, and a guess a second source clears stays flagged forever, meaning the review queue can only grow. Live effect: 62 rows now carry two or more sources and the queue fell from 169 to 150.

## Verified against the live Zest tenant (2026-09-03)

The README's first-run checklist has been run against Zest's real account (`zestlawnpest.fieldroutes.com`, office 1), `FR_WRITE_CUSTOMER_IDS=[10000]` (customer "Test Sean") throughout. Results:

- `health_check`, `lookups`, `find_customer`/`customer_360`, `day_schedule`, `crew_schedule` all correct against real data on the first try.
- **Red Notes resolved** -- see the field-names note above. `customer_alerts`/`set_red_notes` needed no changes.
- **`subscription/update` does a true partial update** -- confirmed by changing one field (`duration` 15→16) on a real subscription and reading every other field back byte-identical, then reverting. This was the one open question that could have silently corrupted a subscription; it's safe.
- **Found and fixed live**: `add_note`'s `note_type = note_type_id or _default_note_type()` dropped an explicitly-passed `note_type_id=0` (Python falsy-zero) -- and Zest's account has a real Note Type with ID 0 ("Notes", Admin → Preferences → Note Types). Fixed to an explicit `is not None` check. Checked every other `x or _default_y()` pattern in `server.py`: `employee_id or _default_employee()` (two call sites) is fine as-is, since `0` is FieldRoutes' own "unassigned" sentinel for employee fields everywhere else in the live API (confirmed against real `assignedTech`/`preferredTechID` values) -- never a real employee ID at this office (real ones start at 10000).
- **Real blocker, not a code bug**: every `spot` office-wide (past, present, and future) has `apiCanSchedule: "0"` -- confirmed via a direct `spot/search` with no date/route restriction. This is exactly the risk this file already flagged ("API key lacking 'can schedule' permission"). `schedule_appointment`/`open_slots`/`reserve_slot` can't be meaningfully tested until this is granted in FieldRoutes Admin → API → Manage Keys (or whatever office-wide setting controls it) -- ask the owner before assuming it's the API key specifically.
- **Fixed**: `due_for_service`'s default `limit=200` produced a 200K+ character response against Zest's real subscription volume -- too large for a single tool response in practice. Default lowered to 50 (still fully overridable -- pass a higher `limit` once narrowed by service type/region/tech for a bigger pull), and its output now uses `DUE_FOR_SERVICE_KEYS`, a trimmed subset of `SUBSCRIPTION_KEYS` dropping billing/sales/administrative fields (`soldBy*`, `billingFrequency`, `contractValue`, `agreementLength`, etc.) that a "what's due" skim doesn't need. `subscription_details`/`service_schedule` are untouched -- they still pass the full `SUBSCRIPTION_KEYS` to `_shape_subscriptions` via its new optional `keys` param.
- Confirmed live: `subscriptionID: "-1"` (the stand-alone sentinel) appears in real appointment data (e.g. appointment 16707), corroborating the `service_schedule` fix that excludes it from `subscription/get` lookups.
- **Second pass against real customers (14699, 14964, 19741 -- read-only) found a whole bug class the fake couldn't see, all fixed**: every live field value is a *string*, so `"0"` is truthy -- `route_stops` labeled every open spot "reserved", `_shape_subscriptions` rendered `region: "0"` for unassigned regions and emitted `soldBy2Name: null` noise. Rule: **never truth-test a raw FieldRoutes field; go through `_int()`** (`open_slots` already did; `route_stops` didn't). Same pass: `frequency` arrives as the literal string `"CUSTOM"` for custom-schedule subscriptions (not `-3`); `customer/search`'s `phone` param is an exact 10-digit match across phone1/phone2/additional contacts and **neither `phone` nor `phone1` honors CONTAINS** (returns zero rows silently -- `find_customer(phone=...)` was returning nothing on live until fixed to send a bare normalized number; partial numbers can't be searched at all); an appointment's `employeeID` is who *booked* it (office staff, or `FR-System-Job` for imports), not the tech -- `_shape_appointments` now falls back to the route's tech instead and reports `bookedBy` separately; live names/addresses carry stray whitespace (`"Pedro "`, a leading tab), normalized via `_clean()`. `tests/conftest.py`'s seed now carries a string-valued spot (35) and subscription (101) so the fake models these shapes.
- **Write pass (customer 10000 only), every call read back afterwards**: `add_note`, `update_note`, `set_red_notes`, `update_subscription`, `reschedule_appointment` (duration/time-window only -- no spot needed), `update_appointment_notes`, `create_task`, and generic `call` writes (`appointment/update`, `task/update`) all verified live. Three things only a real write could show, all fixed: (1) `appointment/update` has **no office-notes param** -- the tool sent `officeNotes`, FieldRoutes answered `success: true` with `ignoredParams: ["officeNotes"]` and wrote nothing; `office_notes` is gone from `update_appointment_notes` (`appointment/complete` does accept `officeNotes`, so `complete_appointment` keeps it). (2) That silent-drop pattern is now an error everywhere: `client.call()` raises `FieldRoutesError` when a param we sent shows up in `ignoredParams` -- for a write that means a partial save, for a search an ignored filter means *unfiltered* rows. `FakeFR.ignore_params` models it. (3) `task/create` **requires** `category`, rejects `0` and the spec's built-in IDs (`10` → "not a valid taskCategoryID for the office"), and only accepts the office's own Task Categories, which have no listing endpoint -- Zest's only one is `10002 "Billing"`; every human-made task otherwise carries `category "0"`. `create_task` now falls back to `FR_DEFAULT_TASK_CATEGORY_ID` and errors clearly, and `lookups(kind="task_categories")` derives the in-use IDs from existing tasks. Also confirmed: the `notes` param of `appointment/update` lands in the record's `appointmentNotes`, and an empty string clears it.
- **`cancel_appointment` verified live** (appointment 10002, customer 10000, General Service, 2026-07-21): cancelled, read back with `status: "-1"`/`statusText: "Cancelled"`, `dateCancelled` stamped, `cancelledBy` matching `FR_DEFAULT_EMPLOYEE_ID`. Confirmed `APPOINTMENT_STATUS`'s existing `{-1: "cancelled"}` entry resolves correctly (`-1` is truthy in Python, so `_int(status) or 0` evaluates to `-1`, not `0` -- no falsy-sentinel bug here, unlike the `note_type_id=0` one above).
- **Found and fixed live, from that same cancel test**: `customer_360` (and `customer_alerts`, same pattern) sends `customer/get` best-effort `include*` params the swagger declares (`includeSubscriptions`, `includeCustomerFlag`, `includeAdditionalContacts`) that Zest's live account silently ignores -- and the new `ignoredParams`-is-an-error guard above (correctly) turned that into a hard failure, even though both tools already have working fallbacks for exactly this case (a separate `subscription/get`, `flags` defaulting to `[]`). Rather than weaken the guard, `client.call()`/`client.get()`/`_get_rows()` gained an `optional_params` parameter: names sent best-effort, with a real fallback, that are allowed to appear in `ignoredParams` without raising -- while any *other*, unexpected ignored param still raises. `customer_360`/`customer_alerts` now pass their three include params as optional; the generic `get`/`call` tools don't, so an ignored `include` there still surfaces to the caller (no fallback exists to make that safe to swallow).
- `reserve_slot`→`schedule_appointment` still blocked on the scheduling-permission gap above. `complete_appointment` not yet run -- it generates a real invoice and this office has a QuickBooks integration (seen in Admin → Preferences), so it needs an explicit go-ahead on a disposable appointment, not just a general "test writes" one.
- Still open: whether scheduling/rescheduling/cancelling triggers a customer-facing SMS/email depending on office settings (untestable until scheduling is unblocked); `.pestroutes.com` vs `.fieldroutes.com` is resolved (`.fieldroutes.com`, confirmed by `baseUrl` in every `health_check`).
- **2026-09-07: `FR_WRITE_CUSTOMER_IDS` cleared at the owner's explicit request** -- the deployment is live, writes are unrestricted across the whole office (verified via `health_check`'s `writeCustomerAllowlist: null`). `FR_ALLOW_DELETE`/`FR_ALLOW_CHARGES` stay off, unaffected by this. `reserve_slot`/`schedule_appointment`/`complete_appointment` are still unverified against a real write for the reasons above -- that risk is now live-wide, not contained to a test customer, so take extra care before exercising those for the first time.

## Owner context

Zest Lawn & Pest (Sacramento): lawn fertilization/weed control, recurring pest, rodent exclusion, irrigation, mowing. Owner thinks in unit economics and route density; tools that surface density (lat/lng, distance to previous/next stop) are worth keeping. Office notes should stay to two sentences.
